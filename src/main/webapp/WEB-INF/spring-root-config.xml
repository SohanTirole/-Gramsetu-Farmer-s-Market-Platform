<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/tx
         http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- Scan service, dao, config, util layers (NOT controllers — those go in MVC config) -->
    <context:component-scan base-package="
        com.gramsetu.service,
        com.gramsetu.dao,
        com.gramsetu.config,
        com.gramsetu.util"/>

    <!--
        Property placeholder: reads db.properties from classpath.
        system-properties-mode="ENVIRONMENT" means OS environment variables
        (DB_URL, DB_USERNAME, DB_PASSWORD etc.) will OVERRIDE whatever is in db.properties.
        This is how Render injects secrets at runtime.
    -->
    <context:property-placeholder
        location="classpath:db.properties"
        system-properties-mode="ENVIRONMENT"
        ignore-unresolvable="true"/>

    <!-- DataSource: values come from env vars on Render, from db.properties in local dev -->
    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${db.driver}"/>
        <property name="url"             value="${db.url}"/>
        <property name="username"        value="${db.username}"/>
        <property name="password"        value="${db.password}"/>
    </bean>

    <bean id="jdbcTemplate"
          class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <bean id="transactionManager"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- BCrypt password encoder -->
    <bean id="passwordEncoder"
          class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <!-- JavaMail: optional — only works if MAIL_USERNAME and MAIL_PASSWORD are set -->
    <bean id="mailSender"
          class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host"     value="${mail.host:smtp.gmail.com}"/>
        <property name="port"     value="${mail.port:587}"/>
        <property name="username" value="${mail.username:}"/>
        <property name="password" value="${mail.password:}"/>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.starttls.enable">true</prop>
                <prop key="mail.smtp.connectiontimeout">5000</prop>
                <prop key="mail.smtp.timeout">5000</prop>
            </props>
        </property>
    </bean>

</beans>
