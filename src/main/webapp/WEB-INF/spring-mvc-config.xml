<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/mvc
         http://www.springframework.org/schema/mvc/spring-mvc.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Scan controllers only -->
    <context:component-scan base-package="com.gramsetu.controller"/>

    <!-- Enable MVC annotations (auto-registers mvcHandlerMappingIntrospector) -->
    <mvc:annotation-driven/>

    <!-- Serve static files from webapp/static/ -->
    <mvc:resources mapping="/static/**" location="/static/"/>

    <!--
      Serve user-uploaded files from the filesystem.
      On Render, files are written to ${user.home}/gramsetu-uploads/
      The URL /uploads/** maps to that directory.
    -->
    <mvc:resources mapping="/uploads/**"
                   location="file:#{systemProperties['user.home']}/gramsetu-uploads/"/>

    <!-- JSP View Resolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
        <property name="order"  value="1"/>
    </bean>

    <!-- Multipart File Upload (Jakarta / Servlet 3.0+) -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.support.StandardServletMultipartResolver"/>

</beans>
